You want to upload your project to web and need to put it in public folder?? Let's go

1 - put the `index.php` here in `public/` folder

2 - put the `assts` folder here in `public/` folder

3 - uncomment the line 2 and 6 in `.htaccess` by removing the `#`
    - comment the line 4 and 5 in the `.htaccess` by adding the `#` at the beginning of the line